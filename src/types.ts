export interface PortfolioImgGlryPhoto{
    src: string,
    caption: string,
    full_desc: string
}

export interface Desc {
    sessionTitle: string,
    text: string
}

export type LogoRestModeProps = {
    activated: boolean
}